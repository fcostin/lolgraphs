ugly little python script for pulling json data and graphing it

it would be nicer ...

	* to have more data (could accumulate this by querying over time ...)
	* not to hammer their site every time this is run (caching ...)

but whatever.
